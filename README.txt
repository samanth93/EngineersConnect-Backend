# EngineersConnect-Backend

1. Download the Backend-end project
2. Open command prompt/terminal and go the project directory
3. execute=> npm install or sudo npm install, to install all dependencies
4. execute => node index.js, to start the server 
5. Use Vscode to open project 
